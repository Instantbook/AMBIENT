# Build output — regenerate with: bash app/build.sh
app/build/

# Signing keys never belong in the repo
*.keystore
*.jks

# wrangler's local state
.wrangler/

# Device-local config baked into the APK at build time, never published
app/worker.url
